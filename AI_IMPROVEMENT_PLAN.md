# AI_IMPROVEMENT_PLAN.md — 改善方針記録

## サイクル1（2026-07-15）

### 目標体験
初見プレイヤーが事前知識ゼロで Day 1〜3 を「理不尽さなく・操作に迷わず」楽しめる状態にする。

### 採用（今サイクルで実施）— テーマ: 初見オンボーディングと理不尽の除去

**A. Day 1 オンボーディング＋目標の常時明示【Codex提案1／Claude分析①③】**
- Day 1（ストーリー）の main_game 開始時に、操作ガイドを段階表示するオーバーレイ/ヒントを追加:
  「ピースをタップで選択 → もう一度タップで回転 → 箱をタップで配置」「全部収納してから封印しよう」。
- HUD付近に常時「未収納 N個 / 封印時 SAN −X」を表示（全Day共通）。全収納時は「封印できる！」表示に変化。
  → 数字バッジの意味（=未収納時のSANダメージ）とゲーム目標が自然に伝わる。
- セーブ形式変更を避けるため、既読フラグは保存しない（Day 1のみ表示という条件で十分）。
- 理由: Claude・Codex双方が最優先と判断。Day 3が回転必須である以上、回転の教示は3ステージ到達の必要条件。

**B. 配置失敗フィードバックの適正化【Codex提案5／Claude分析②】**
- `_on_grid_item_rejected()` からジャンプスケア呼び出しを除去。振動＋既存の赤パルス＋短いヒント文言
  （例:「そこには置けない」）に置き換え。
- ジャンプスケアはタイムアップ強制封印（既存 main_game.gd:169）等の重大イベントに温存
  → Phase 4計画「ジャンプスケアを大ミス/限界時のみに集約」とも整合。
- エラーSEアセットは追加しない（アセット追加は今回見送り。人間確認項目に「エラーSE素材の用意」を記載）。
- 理由: 初見は必ず配置ミスする。学習ミスへの1.2秒スケア連発はテンポ・恐怖価値の両方を毀損。双方の分析が一致。

**C. Day 3 呪いセルの公平化【Codex提案2（Codex独自の発見）】**
- 呪いセルの初出現を Day 4 以降に変更（初の時間制限ステージ Day 3 は表示どおり60秒使える）。
- 呪いセル出現の3秒前から対象セルを点滅警告（既存計画「呪いセル出現警告」の前倒し）。
- リセット時に動的追加された呪いセルを解除（stage初期の blocked_cells は維持）。
- 理由: 16マス/16セルのDay 3で封鎖1つ=全収納不可能は、初見には「理由の分からない失敗」。
  コアルール（呪いセルという仕組み）は維持し、出現タイミングの調整のみ。

### 不採用（今サイクルでは見送り・理由つき）
| 項目 | 判断 | 理由 |
|------|------|------|
| VNスキップ・高速閲覧（Codex7） | サイクル2候補 | 効果は高いがオンボーディングと別システム。1サイクル1機能単位の制約を守る |
| 事件資料とピースの同期（Codex4） | サイクル2候補 | シナリオテキストの品質に関わるため未検証。次サイクルで内容を検証してから実施 |
| トレイ個体ID（Codex5） | サイクル2〜3候補 | 選択・削除ロジックの改修はリグレッションリスクがあり、単独サイクルで慎重に実施したい |
| 事件資料の閉じるボタン（Codex8） | サイクル2候補 | 小規模だが今サイクルのテーマ外。VN改善とまとめて実施が効率的 |
| 封印確認ダイアログ（Codex6） | 部分採用 | 常時「封印時 SAN −X」表示（採用A）で誤タップの実害を大幅軽減。ダイアログ追加はテンポ悪化の懸念があり見送り |
| Day 2 難易度緩和（Claude分析④） | 撤回 | 事前調査の誤り。Day 2は時間無制限・全埋め可能で問題なし（AI_REVIEW.md参照） |

### 変更規模見積り
対象ファイル（想定8件・上限20以内）: main_game.gd / main_game.tscn / hud.gd / hud.tscn /
grid_box.gd / game_manager.gd（呪い開始Day判定）/ 新規 tutorial_overlay（.gd/.tscn）程度。

### 制約遵守
- コアルール（グリッド配置・SAN・封印・呪いセルの存在）は不変。調整はタイミングと提示方法のみ。
- 依存追加・セーブ形式変更・アセット削除・外部通信追加: なし。

---

## サイクル2（2026-07-15）人間フィードバック対応

### フィードバック
「ジャンプスケア演出が消えています。これはホラーとして大切な演出です。復元してください。」

### 判断
- **採用: 配置失敗時のジャンプスケアを復元**（サイクル1変更Bの部分取り消し）。
  ジャンプスケアはこのゲームのホラーアイデンティティの核であり、その存否はディレクター（人間）の判断に従う。
  Day 1・2は時間無制限のためタイムアップスケアも発生せず、序盤からスケアがほぼ消えていた点で
  フィードバックは妥当。
- **維持: フィードバック対象外のサイクル1改善**。ヒント「そこには置けない」表示と
  クリック配置中の選択維持は、スケア演出と共存可能でありオンボーディング効果を保つため残す。
  ガイド表示・封印情報表示・呪いセル公平化もそのまま。
- 実装承認: 人間からの明示的な修正指示（「実施してください」）をもって承認済みとする。

---

## サイクル3（2026-07-15）人間フィードバック対応: 緊迫感演出（AnomalyDirector）

### フィードバック（原文要約）
①残り時間減少で画面エフェクト＋BGMの緊迫感 ②ミスすると机にお化けの手 ③時間経過で怪異が増える
④ピースが少しずつ「明らかに様子がおかしい」見た目になる ⑤制限時間で段々視野が狭くなる
⑥足りない素材はCodexに作らせる
→ DEV_LOG記載のPhase 4「AnomalyDirector」計画の前倒し実装に相当。

### 採用（全項目・実装方式はCodexレビュー準拠）
新規ノード **AnomalyDirector**（既定レイヤー z_index=12 の全画面Control）に演出を集約。
残り時間比率から緊張度 tension（残り60%から0〜1で上昇、時間無制限Dayは常に0）を算出し一元制御:

1. **視野狭窄＋画面エフェクト**: ColorRect 1枚＋gdshader 1本（ビネット暗転・赤み・明滅を統合。
   TIMEは使わずMainGameのdeltaで進む effect_time をuniform渡し。SCREEN_TEXTURE不使用）
2. **BGM緊迫感**: `AudioManager.set_bgm_pitch()` 新設。tension平滑値で pitch_scale 1.0→1.07。
   封印・リセット・タイトル遷移・Day切替で必ず1.0へ復帰
3. **怪異の増加**: 怪異イベント（ピース異変/画面明滅スパイク）の発生間隔を tension で 14秒→5秒に短縮
4. **ピース異変**: `ItemVisual` に glitch 描画（_draw内のdraw_set_transformで震え±3px・傾き±3度・
   変色。ノードのrotation/scale/size/形状データは不変）。対象はトレイ内のItemVisualインスタンス・同時1個
5. **お化けの手（ミス時）**: 配置失敗の約1.2秒後（ジャンプスケア終了後）に画面下端から手のシルエットが
   出現→引っ込む。時間無制限Dayでも発動。ジャンプスケア自体は不変（前回フィードバック遵守）
6. **不足素材**: お化けの手SVG（テキスト形式・透過）を新規作成。Codexに作成を依頼し、
   失敗時はClaudeが作成。バイナリ素材（緊迫BGM・手専用SE）は今回見送り、人間確認項目に記載

### 不採用・見送り
| 項目 | 理由 |
|------|------|
| 配置済みピース（盤面側）の異変 | GridBox._draw側の別実装が必要。まずトレイ内のみ（Codex推奨の低リスク構成）。効果を見て次回拡張 |
| 緊迫BGM差し替え/ステム・手専用SE | バイナリ音声素材が必須（AIはテキストしか作成できない）。pitch_scaleで代替し、素材は人間に依頼 |
| SceneTree.paused への移行 | ポーズ機構の作り替えはリスク大。演出時計をdelta供給方式にして回避 |

### 変更規模見積り（9ファイル・上限20以内）
main_game.gd / main_game.tscn / audio_manager.gd / item_visual.gd / item_tray.gd /
新規: anomaly_director.gd / anomaly_director.tscn / anomaly_overlay.gdshader / ghost_hand.svg

### 制約遵守
- コアルール・形状データ・SAN計算・封印フロー・ジャンプスケア: 不変（見た目と音の演出のみ）
- セーブ形式変更・依存追加・アセット削除・外部通信: なし。新規アセット追加はSVG/シェーダー（テキスト）のみ
