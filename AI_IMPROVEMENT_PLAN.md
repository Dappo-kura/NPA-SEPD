# AI_IMPROVEMENT_PLAN.md — 改善方針記録

# ══ UI・UX改善ループ（2026-07-16開始・最大3サイクル）══

## UI・UXサイクル2（2026-07-17）

### 目標体験
「まずい状況」が数字を読まなくても伝わり、死の瞬間がゲーム中最も恐ろしい演出になる状態にする。
（危機の可視化＋ホラー演出強化。Claude分析とCodexレビューのトップ3が完全一致した領域）

### 採用（今サイクル）— テーマ: 危機の可視化とリザルト演出

**A. タイマーを「秒数が読める警報」に【Codex1／Claude候補1・一致】**
- タイマーバー直下（または横）に残り秒数を常時表示（例「残り 42秒」、終盤は小数なし整数）。
- 残り10秒: 数字を赤く拡縮パルス。残り3秒: グリッドの赤ボードグロー（既存 pulse_reject 系描画流用）で最終警告。
- 時間無制限Dayは非表示（従来どおり）。

**B. 緊張度をSANと合成【Codex2・Codex独自】**
- `_current_tension()` を `max(時間危機, SAN危機)` に変更。SAN危機はSAN30以下から0に向けて0→1。
- 既存のビネット・BGMピッチ・怪異頻度・ピースグリッチがそのままSAN低下にも反応する（演出資産の入力追加のみ）。
- 時間無制限DayでもSANが減っていれば不穏さが出る。予測封印後SANの項は今回見送り（配置のたびに演出が上下して騒がしくなる懸念。効果を見て次回検討）。

**C. 呪いセル警告の因果明示【Codex3／Claude候補3・一致】**
- 警告セルに残り秒カウント「3→2→1」を描画（GridBox._draw内・フォント描画のみ）。
- 呪い警告の初回発生時のみヒント「紫のマスが呪われる――埋めれば防げる」を表示（既存 _show_hint 流用）。
- 封鎖確定時は紫系の専用パルス（配置ミスの赤 pulse_reject と判別可能に）。警告中セルへの配置成功（防御）時は明るい逆パルス。

**D. 死の演出強化【Claude候補2＋Codex4の合成】**
- nightmare_event: SAN表示を「SAN −X」から「SAN 18 → 3」形式に変更（適用前SANは main_game から渡す。残存SANで色: 緑/黄/赤）。SAN 0時は「SAN 12 → 0」を赤グリッチ表示。
- game_over: `_ready()` でBGM停止・黒背景＋赤ビネット（anomaly_overlay.gdshader 流用またはColorRectグラデ）・「GAME OVER」1.5秒フェードイン→その後ボタン表示。
- nightmare_event.show_event のシグネチャ変更は main_game 側の呼び出し2箇所も同時更新（互換性維持）。

**E. gallery.gd の ScreenTouch 残存修正【Claude独自・サイクル1修正Bの取り残し】**
- gallery.gd:57 のタップ判定をマウスイベントのみに統一（1ファイル・小）。

### 不採用・持ち越し（理由つき）
| 項目 | 判断 | 理由 |
|------|------|------|
| 遷移の恐怖の橋（Codex5） | サイクル3候補 | 5-7ファイルの中規模。今サイクルはリザルト系に集中 |
| エンディング差し替え（Codex6） | サイクル3候補＋人間関与 | 本文は物語の結末でありディレクターの創作判断が必要。AI草案→人間承認の形で次サイクル提案 |
| 配置失敗演出の強度整理（Codex7） | **不採用（人間判断事項）** | スケアの表示時間・振幅変更はジャンプスケア演出の変更。事前承認必須のため AI 判断では実施しない（確認項目に記載済み） |
| ギャラリー怪異スチル（Codex8） | サイクル3候補 | 演出強化として有効だが今サイクルのテーマ（危機可視化）外 |
| 予測封印後SANのtension合成（Codex2の一部） | 見送り | 配置/取り出しのたびに演出強度が変動し騒がしい。まず時間+SANで効果確認 |

### 変更規模見積り（9ファイル・上限20以内）
hud.gd / hud.tscn / main_game.gd / grid_box.gd / nightmare_event.gd /
game_over.gd / game_over.tscn / gallery.gd /（必要なら anomaly_director.gd）

### 制約遵守
- ジャンプスケア（発火条件・表示時間・振幅）: 一切変更しない
- コアルール・SAN計算・セーブ形式・依存・外部通信: 変更なし。新規素材: なし（既存シェーダー/描画の流用のみ）


## UI・UXサイクル1（2026-07-16）

### 目標体験
「操作して困惑する瞬間」をなくす。タップ・選択・確定という基本操作のすべてで、ゲームが正しく応答し、取り返しのつかない誤操作が起きない状態にする。

### 採用（今サイクル）— テーマ: 入力フィードバックと誤操作防止

**A. 事件資料画面の操作改修【レビュー#1／Claude候補2・一致】**
- 全面タップ即退出を廃止し、画面下部に「捜査を開始する」ボタンを常設。スクロールと遷移の衝突を解消。
- 上部436pxの空白に事件資料らしいヘッダ装飾（テキスト＋ColorRect。例:「極秘 / CONFIDENTIAL」風の帯）を追加 → 視認性＋ホラーの文脈強化。
- 理由: 毎Day必ず通る画面の機能不全に近い問題。レビュアーもトップ1指名。

**B. タップ送りの二重発火修正【レビュー#2・レビュアー独自発見】**
- day_intro / nightmare_event / kaiki_still / case_file のタップ判定を `InputEventMouseButton` に統一
  （`emulate_mouse_from_touch` デフォルトtrueのため、Android実機で1タップが ScreenTouch + エミュレートMouse の2回処理される疑い）。
- 理由: 事実ならVNテキストが毎回読み飛ばされる実機バグ。修正はローリスク（イベント種の統一のみ）。実機確認項目に記載。

**C. リセット・封印の誤操作防止【レビュー#4】**
- リセット押下時に確認オーバーレイ「配置をすべて戻しますか？」を追加。
- 封印は未収納ピースがある時のみ確認「未収納N個 SAN−X。封印しますか？」を挟む（全収納時は即実行でテンポ維持）。
- 理由: 中断/リセット/封印が横並びで誤タップの代償が大きい。前回「封印確認は見送り」としたが、
  「未収納あり時のみ」という条件付きなら テンポ悪化懸念と両立できる（レビュー案が上位互換）。

**D. 選択中ピースの無反応解消＋危険度バッジの意味明示【レビュー#8】**
- 回転不可ピースの再タップ時にヒント「この証拠品は回転できない」＋一瞬のグリッチ揺れを表示。
- 危険度バッジの表記を「-8」→「SAN-8」に変更し、封印ダメージとの対応を明示。
- 理由: Day1ガイドが「再タップで回転」と教えるため、回転不可ピースが「バグ？」に見える。

### 不採用・持ち越し（理由つき）
| 項目 | 判断 | 理由 |
|------|------|------|
| タイマー数値＋終盤警告（レビュー#3） | サイクル2採用予定 | 効果大だが「危機の可視化＋ホラー演出」としてゲームオーバー演出・呪い警告とまとめる方が一貫する |
| ゲームオーバー画面演出（#6） | サイクル2採用予定 | 同上（演出系サイクルに集約） |
| 呪い警告の初回説明（#7) | サイクル2採用予定 | 同上 |
| 同一Day再訪スキップ（#9） | サイクル3候補 | テンポ系。広告リトライ導線と絡むため単独で慎重に |
| SEプール化（#10） | サイクル3候補 | Phase 3計画と同一。演出が増えるサイクル2の後に入れると効果測定しやすい |
| スケア発火条件の絞り込み（#5） | **不採用（人間判断事項）** | ジャンプスケアはホラーの核・変更は事前承認必須（前回人間フィードバック）。AIからは再提案しない。人間確認項目に「検討事項」として記載のみ |
| タイトル画面ホラー演出（Claude候補6） | バックログ | レビューで挙がらず優先度低。サイクル3で余力があれば |
| 設定にテキスト速度/演出強度（Claude候補7） | バックログ | 同上 |
| SE素材追加（Claude候補8） | 人間依頼 | バイナリ素材はAI作成不可。人間確認項目に記載 |

### 変更規模見積り（8ファイル・上限20以内）
case_file.gd / case_file.tscn / day_intro.gd / nightmare_event.gd / kaiki_still.gd /
main_game.gd / main_game.tscn / item_visual.gd

### 制約遵守
- コアルール（配置・SAN・封印・呪いセル・ジャンプスケア）: 不変。ジャンプスケアには一切触れない。
- セーブ形式変更・依存追加・アセット削除・外部通信: なし。新規素材: なし（既存UIノードのみ）。


## サイクル1（2026-07-15）

### 目標体験
初見プレイヤーが事前知識ゼロで Day 1〜3 を「理不尽さなく・操作に迷わず」楽しめる状態にする。

### 採用（今サイクルで実施）— テーマ: 初見オンボーディングと理不尽の除去

**A. Day 1 オンボーディング＋目標の常時明示【Codex提案1／Claude分析①③】**
- Day 1（ストーリー）の main_game 開始時に、操作ガイドを段階表示するオーバーレイ/ヒントを追加:
  「ピースをタップで選択 → もう一度タップで回転 → 箱をタップで配置」「全部収納してから封印しよう」。
- HUD付近に常時「未収納 N個 / 封印時 SAN −X」を表示（全Day共通）。全収納時は「封印できる！」表示に変化。
  → 数字バッジの意味（=未収納時のSANダメージ）とゲーム目標が自然に伝わる。
- セーブ形式変更を避けるため、既読フラグは保存しない（Day 1のみ表示という条件で十分）。
- 理由: Claude・Codex双方が最優先と判断。Day 3が回転必須である以上、回転の教示は3ステージ到達の必要条件。

**B. 配置失敗フィードバックの適正化【Codex提案5／Claude分析②】**
- `_on_grid_item_rejected()` からジャンプスケア呼び出しを除去。振動＋既存の赤パルス＋短いヒント文言
  （例:「そこには置けない」）に置き換え。
- ジャンプスケアはタイムアップ強制封印（既存 main_game.gd:169）等の重大イベントに温存
  → Phase 4計画「ジャンプスケアを大ミス/限界時のみに集約」とも整合。
- エラーSEアセットは追加しない（アセット追加は今回見送り。人間確認項目に「エラーSE素材の用意」を記載）。
- 理由: 初見は必ず配置ミスする。学習ミスへの1.2秒スケア連発はテンポ・恐怖価値の両方を毀損。双方の分析が一致。

**C. Day 3 呪いセルの公平化【Codex提案2（Codex独自の発見）】**
- 呪いセルの初出現を Day 4 以降に変更（初の時間制限ステージ Day 3 は表示どおり60秒使える）。
- 呪いセル出現の3秒前から対象セルを点滅警告（既存計画「呪いセル出現警告」の前倒し）。
- リセット時に動的追加された呪いセルを解除（stage初期の blocked_cells は維持）。
- 理由: 16マス/16セルのDay 3で封鎖1つ=全収納不可能は、初見には「理由の分からない失敗」。
  コアルール（呪いセルという仕組み）は維持し、出現タイミングの調整のみ。

### 不採用（今サイクルでは見送り・理由つき）
| 項目 | 判断 | 理由 |
|------|------|------|
| VNスキップ・高速閲覧（Codex7） | サイクル2候補 | 効果は高いがオンボーディングと別システム。1サイクル1機能単位の制約を守る |
| 事件資料とピースの同期（Codex4） | サイクル2候補 | シナリオテキストの品質に関わるため未検証。次サイクルで内容を検証してから実施 |
| トレイ個体ID（Codex5） | サイクル2〜3候補 | 選択・削除ロジックの改修はリグレッションリスクがあり、単独サイクルで慎重に実施したい |
| 事件資料の閉じるボタン（Codex8） | サイクル2候補 | 小規模だが今サイクルのテーマ外。VN改善とまとめて実施が効率的 |
| 封印確認ダイアログ（Codex6） | 部分採用 | 常時「封印時 SAN −X」表示（採用A）で誤タップの実害を大幅軽減。ダイアログ追加はテンポ悪化の懸念があり見送り |
| Day 2 難易度緩和（Claude分析④） | 撤回 | 事前調査の誤り。Day 2は時間無制限・全埋め可能で問題なし（AI_REVIEW.md参照） |

### 変更規模見積り
対象ファイル（想定8件・上限20以内）: main_game.gd / main_game.tscn / hud.gd / hud.tscn /
grid_box.gd / game_manager.gd（呪い開始Day判定）/ 新規 tutorial_overlay（.gd/.tscn）程度。

### 制約遵守
- コアルール（グリッド配置・SAN・封印・呪いセルの存在）は不変。調整はタイミングと提示方法のみ。
- 依存追加・セーブ形式変更・アセット削除・外部通信追加: なし。

---

## サイクル2（2026-07-15）人間フィードバック対応

### フィードバック
「ジャンプスケア演出が消えています。これはホラーとして大切な演出です。復元してください。」

### 判断
- **採用: 配置失敗時のジャンプスケアを復元**（サイクル1変更Bの部分取り消し）。
  ジャンプスケアはこのゲームのホラーアイデンティティの核であり、その存否はディレクター（人間）の判断に従う。
  Day 1・2は時間無制限のためタイムアップスケアも発生せず、序盤からスケアがほぼ消えていた点で
  フィードバックは妥当。
- **維持: フィードバック対象外のサイクル1改善**。ヒント「そこには置けない」表示と
  クリック配置中の選択維持は、スケア演出と共存可能でありオンボーディング効果を保つため残す。
  ガイド表示・封印情報表示・呪いセル公平化もそのまま。
- 実装承認: 人間からの明示的な修正指示（「実施してください」）をもって承認済みとする。

---

## サイクル3（2026-07-15）人間フィードバック対応: 緊迫感演出（AnomalyDirector）

### フィードバック（原文要約）
①残り時間減少で画面エフェクト＋BGMの緊迫感 ②ミスすると机にお化けの手 ③時間経過で怪異が増える
④ピースが少しずつ「明らかに様子がおかしい」見た目になる ⑤制限時間で段々視野が狭くなる
⑥足りない素材はCodexに作らせる
→ DEV_LOG記載のPhase 4「AnomalyDirector」計画の前倒し実装に相当。

### 採用（全項目・実装方式はCodexレビュー準拠）
新規ノード **AnomalyDirector**（既定レイヤー z_index=12 の全画面Control）に演出を集約。
残り時間比率から緊張度 tension（残り60%から0〜1で上昇、時間無制限Dayは常に0）を算出し一元制御:

1. **視野狭窄＋画面エフェクト**: ColorRect 1枚＋gdshader 1本（ビネット暗転・赤み・明滅を統合。
   TIMEは使わずMainGameのdeltaで進む effect_time をuniform渡し。SCREEN_TEXTURE不使用）
2. **BGM緊迫感**: `AudioManager.set_bgm_pitch()` 新設。tension平滑値で pitch_scale 1.0→1.07。
   封印・リセット・タイトル遷移・Day切替で必ず1.0へ復帰
3. **怪異の増加**: 怪異イベント（ピース異変/画面明滅スパイク）の発生間隔を tension で 14秒→5秒に短縮
4. **ピース異変**: `ItemVisual` に glitch 描画（_draw内のdraw_set_transformで震え±3px・傾き±3度・
   変色。ノードのrotation/scale/size/形状データは不変）。対象はトレイ内のItemVisualインスタンス・同時1個
5. **お化けの手（ミス時）**: 配置失敗の約1.2秒後（ジャンプスケア終了後）に画面下端から手のシルエットが
   出現→引っ込む。時間無制限Dayでも発動。ジャンプスケア自体は不変（前回フィードバック遵守）
6. **不足素材**: お化けの手SVG（テキスト形式・透過）を新規作成。Codexに作成を依頼し、
   失敗時はClaudeが作成。バイナリ素材（緊迫BGM・手専用SE）は今回見送り、人間確認項目に記載

### 不採用・見送り
| 項目 | 理由 |
|------|------|
| 配置済みピース（盤面側）の異変 | GridBox._draw側の別実装が必要。まずトレイ内のみ（Codex推奨の低リスク構成）。効果を見て次回拡張 |
| 緊迫BGM差し替え/ステム・手専用SE | バイナリ音声素材が必須（AIはテキストしか作成できない）。pitch_scaleで代替し、素材は人間に依頼 |
| SceneTree.paused への移行 | ポーズ機構の作り替えはリスク大。演出時計をdelta供給方式にして回避 |

### 変更規模見積り（9ファイル・上限20以内）
main_game.gd / main_game.tscn / audio_manager.gd / item_visual.gd / item_tray.gd /
新規: anomaly_director.gd / anomaly_director.tscn / anomaly_overlay.gdshader / ghost_hand.svg

### 制約遵守
- コアルール・形状データ・SAN計算・封印フロー・ジャンプスケア: 不変（見た目と音の演出のみ）
- セーブ形式変更・依存追加・アセット削除・外部通信: なし。新規アセット追加はSVG/シェーダー（テキスト）のみ
