# AI_IMPROVEMENT_LOG.md — 改善実施記録

## サイクル1（2026-07-15）初見オンボーディングと理不尽の除去

### 実施した変更（3ファイル・+169/−13行）

実装担当: Claude Code（Codexが2回連続で指示ファイルを処理できなかったため、規約に基づき代行）

**1. Day 1 オンボーディング＋目標の常時明示**
- `scenes/main_game/main_game.tscn`: `GuideLabel`（グリッド直下）と `SealInfoLabel`（封印ボタン直上）を追加
- `scenes/main_game/main_game.gd`:
  - 全Day共通: 「未収納 N個 ／ 封印すると SAN −X」を常時表示、全収納で「封印できる！」（緑）に変化。
    更新はステージ開始/配置成功/トレイ復帰/リセット時のみ（毎フレーム更新なし）
  - 予測ダメージ計算を `_predicted_seal_damage()` に共通化し、封印処理本体も同関数を使用（ロジック重複解消）
  - ストーリーDay 1限定の段階ガイド: 開始時「証拠品をタップして選ぼう」→選択時「もう一度タップで回転 ／
    箱をタップで収納」→初配置後「全て収納したら『封印する』を押そう」→全収納で非表示
  - セーブ形式は不変（既読フラグなし。表示条件は MODE_STORY かつ current_day==1）

**2. 配置失敗フィードバックの適正化**
- `_on_grid_item_rejected()`: ジャンプスケア呼び出しを削除。振動＋既存の赤パルス＋ヒント「そこには置けない」に変更
- クリック配置モード中の失敗はピース選択を維持（そのまま別の場所を再試行できる）。ドラッグ失敗は従来どおりトレイ復帰
- ジャンプスケアはタイムアップ強制封印（main_game.gd）に温存

**3. Day 3 呪いセルの公平化**
- `main_game.gd`: ストーリーモードでは呪いセル出現を Day 4 以降に限定（Day 3 は表示どおり60秒使える）。無限モードは従来どおり
- `scenes/main_game/grid_box/grid_box.gd`: `add_cursed_cell()`（即時封鎖）を廃止し、
  「3秒間の紫点滅警告 → 期限時にそのセルが空いていれば封鎖確定／ピースで守られていればスキップ」方式
  （`begin_cursed_warning` / `tick_cursed_warning` / `_commit_cursed_cell`）に変更。
  警告の進行は main_game 側から delta 供給（ポーズ・封印中は自動停止）
- 動的呪いセルを `_dynamic_cursed` で追跡し、リセットで解除（`clear_cursed_cells`）。ステージ定義の封鎖マスは残す

### テスト・検証結果

| 検証 | 結果 | 信頼度 |
|------|------|--------|
| git diff レビュー（制約違反・バグ・仕様逸脱） | 問題なし。変更3ファイル（上限20以内）、コアルール・セーブ形式・アセット・外部通信に変更なし | 自動検証済み（diff全文確認） |
| 廃止API `add_cursed_cell` への残存参照 | コード内ゼロ（ドキュメント2件のみ） | 自動検証済み（grep） |
| 新規ノードパス（GuideLabel/SealInfoLabel）と @onready の整合 | 一致 | 自動検証済み（grep） |
| 新API（begin/tick/clear）の呼び出し整合 | 一致 | 自動検証済み（grep） |
| Godotヘッドレス起動チェック（tools/check_scenes.gd） | **未実施** — 本セッションの権限制限でGodot実行コマンドが承認ブロック | 未確認 |
| PCK/APK/Webビルド | **未実施** — 同上（build_pck.py 実行も承認ブロック） | 未確認 |

### 評価指標（改善前→改善後）

- 配置失敗1回あたりの強制待ち時間: 約1.2秒（スケア表示）→ 0秒（AI推定・演出秒数はコード定義値より）
- Day 3 の実質使用可能時間: 約45秒（残り15秒で全収納不可能化）→ 60秒（AI推定・コードロジックより）
- 初見への操作説明: 0件 → 3段階ガイド＋常時目標表示（自動検証済み・実装事実）
- ※プレイ時間・クリア率等の実測値は計測していないため記載しない

### 人間確認項目（プレイして確認してください）

1. **Day 1**: ガイド3段階（選択→回転/収納→封印）が操作に追従して切り替わるか。文言・位置・フォントサイズが1080×2400で読みやすいか
2. **全Day**: 「未収納 N個 ／ 封印すると SAN −X」の数値が実際の封印ダメージと一致するか。全収納で「封印できる！」（緑）になるか
3. **配置失敗**: ジャンプスケアが出ないこと。赤パルス＋「そこには置けない」表示。クリック配置中は選択が維持され再試行できること
4. **タイムアップ**: 従来どおりジャンプスケア→強制封印が発生すること（こちらは温存仕様）
5. **Day 3**: 呪いセルが出現しないこと（60秒フルに使えること）
6. **Day 4以降**: 残り30秒以降に紫点滅の警告→3秒後に封鎖。警告中のセルにピースを置くと封鎖が回避されること。リセットで呪いセルが消え、初期配置の封鎖マスは残ること
7. **無限モード**: ガイド・Day制限が混入しないこと（呪いセルは従来どおり＋警告付き）
8. **レイアウト**: ラベル2つの追加でグリッド・トレイが窮屈になっていないか（特に縦2400px以外の実機）
9. **ビルド**: APK＋Web両方のビルド（本セッションでは実行権限がなく未実施。`build_pck.py`→`build_apk.py`）
10. **エラーSE素材**: 配置失敗用の控えめなSEアセットがあれば追加推奨（今回はアセット追加禁止のため見送り）

---

## サイクル2（2026-07-15）人間フィードバック対応: ジャンプスケア復元

### フィードバック
「ジャンプスケア演出が消えています。これはホラーとして大切な演出です。復元してください。」

### 実施した変更（1ファイル・+2行）
- `scenes/main_game/main_game.gd` `_on_grid_item_rejected()`: サイクル1で削除した
  `jump_scare.trigger()` を復元（配置失敗時にジャンプスケア発動）。維持理由をコメントで明記。
- フィードバック対象外のサイクル1改善（ヒント「そこには置けない」・クリック配置中の選択維持・
  Day 1ガイド・封印情報表示・呪いセル公平化）は変更なし。

### 検証結果
| 検証 | 結果 | 信頼度 |
|------|------|--------|
| `jump_scare.trigger()` 発火箇所 | 2箇所（タイムアップ main_game.gd:182 / 配置失敗 main_game.gd:307）＝サイクル1以前と同一構成 | 自動検証済み（grep） |
| diff レビュー | 復元＋コメント追加のみ。他の挙動に影響なし | 自動検証済み |
| Godotヘッドレス起動 / ビルド | 未実施（実行権限ブロック・サイクル1と同様） | 未確認 |

### 人間確認項目（サイクル1の10項目に追加・変更）
- 項目3を差し替え: **配置失敗時にジャンプスケアが従来どおり発動すること**。その後もピース選択が
  維持され（クリック配置時）、ヒント「そこには置けない」が表示されること
- サイクル1の項目4（タイムアップ時スケア）は変更なくそのまま確認

---

## サイクル3（2026-07-15）人間フィードバック対応: 緊迫感演出 AnomalyDirector

### 実施した変更（9ファイル: 新規4・既存5）

実装担当: **Codex**（今回は指示ファイルの処理に成功。素材SVGもCodexが作成）。レビュー: Claude。

**新規**
- `scenes/main_game/anomaly_director/anomaly_director.gd` … 緊張度平滑化・BGM pitch・怪異イベント・
  ピースglitch・お化けの手タイムラインを一元管理。Tween/Timer/await不使用のdelta駆動（独自ポーズ対応）
- `scenes/main_game/anomaly_director/anomaly_overlay.gdshader` … ビネット（視野狭窄）・暗赤の明滅・
  微ノイズ・イベントスパイクを1パスに統合。TIME/SCREEN_TEXTURE不使用。tension=0で完全透明
- `scenes/main_game/anomaly_director/anomaly_director.tscn` … z_index=12・全画面・mouse_filter=2
- `resources/anomaly/ghost_hand.svg` … お化けの手シルエット（テキスト形式・512×768・透過）

**既存変更**
- `main_game.gd/.tscn` … `_current_tension()`（残り60%から0→1、無制限Dayは0）、毎フレーム
  `anomaly_director.tick()`、ミス時 `on_mistake()`（**ジャンプスケアは不変**、終了後1.2秒で手が出現）、
  封印/リセット/タイトル遷移で `stop_all()`
- `audio_manager.gd` … `set_bgm_pitch()`（1.0〜1.1クランプ）、play/stopでリセット
- `item_visual.gd` … glitch描画（_draw内の震え±3px・傾き±0.05rad・変色。形状/入力は不変）
- `item_tray.gd` … `get_item_visuals()` 読み取り専用アクセサ

### フィードバック→実装の対応
| 要望 | 実装 |
|---|---|
| 時間減少で画面エフェクト | 緊張度連動の暗赤ビネット＋明滅（残り60%から徐々に） |
| BGMの緊迫感 | pitch_scale 1.0→1.07（平滑変化・遷移で復帰） |
| ミスで机にお化けの手 | スケア終了後に画面下端から手がスライドイン→保持→退出（無制限Dayでも発動） |
| 時間経過で怪異が増える | 怪異イベント間隔 14秒→5秒（緊張度連動） |
| ピースが様子おかしくなる | トレイ内ピース1個が0.5秒震え・傾き・緑褐色変色（同時1個・個体単位） |
| 視野が段々狭くなる | ビネット内縁が緊張度で 0.62→0.22 に収縮 |
| 足りない素材はCodex | ghost_hand.svg をCodexが作成。バイナリ素材は下記の人間依頼へ |

### 検証結果
| 検証 | 結果 | 信頼度 |
|------|------|--------|
| diff全文レビュー（制約・バグ・サイクル1/2保持） | 問題なし。jump_scare系ファイル無変更を確認 | 自動検証済み |
| 禁止事項（TIME/SCREEN_TEXTURE/Tween/Timer/await） | 不使用を確認 | 自動検証済み（全文読解） |
| ノードパス・シーン参照・item_tray構造整合 | 一致 | 自動検証済み |
| SVGのXML構文 | 妥当（Codex報告＋Claude目視） | AI確認 |
| Godotヘッドレス起動 / ビルド | **未実施**（Claude・Codexとも実行権限ブロック） | 未確認 |

### 人間確認項目（サイクル3追加分）
1. **SVGインポート**: Godotエディタで `ghost_hand.svg` が正しくインポート・表示されるか（初回起動時に.import生成）
2. **Day 3以降（時間制限あり）**: 残り時間60%を切ると周辺が暗くなり始め、残り0に向けて視野が狭まる+赤い明滅が強まるか。効果が強すぎ/弱すぎないか（調整値: シェーダーuniformと `_current_tension()`）
3. **BGM**: 残り時間減少でピッチ/テンポがわずかに上がるか。封印・リセット・タイトル遷移で元に戻るか。Web版（Chrome/Android Chrome/Safari）で音割れ・ループ不具合がないか
4. **お化けの手**: 配置ミス→ジャンプスケア→その後に手が下から出るか。位置・サイズ・不気味さの評価
5. **ピース異変**: 緊張時にトレイのピースが時々震え・変色するか。選択やドラッグの邪魔にならないか
6. **ポーズ**: 中断中に演出（明滅・手・ピース異変）が止まるか
7. **封印演出との重なり**: 怪異スチル・ナイトメア・ポーズ画面にビネットや手が被らないか
8. **性能**: Android実機とWebで残り10秒付近＋スケア重畳時のフレーム落ちがないか
9. **素材の追加依頼（任意）**: より高品質にする場合、写実的な手の透過PNG・緊迫用BGMステム・手専用SEはバイナリ素材が必要（AIはテキスト形式しか作成できない）
