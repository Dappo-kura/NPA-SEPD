# AI_REVIEW.md — Codexレビュー要約と突き合わせ

## UI・UX改善ループ サイクル2（2026-07-17）Codex独立レビュー要約（ホラー演出・危機可視化）

**注**: 今回はCodex CLIが正常動作（サイクル1は2回失敗）。静的レビューのみ・Web版実プレイは未実施と明記あり。

### 提案一覧（Codex優先度順）
| # | 提案 | 規模 | 素材 |
|---|------|------|------|
| 1 | タイマーを「秒数が読める警報」に: 残り秒数常時表示＋残り10秒で赤カウント拡縮＋残り3秒で盤面外周パルス（flicker_spike/ボードグロー流用） | 小(3-4) | なし |
| 2 | AnomalyDirector緊張度を時間だけでなくSAN・予測封印後SANとの合成値に（`tension=max(時間,SAN,予測)`）。SANバー脈動も同位相 | 中(3-5) | なし |
| 3 | 呪いセル警告に「3・2・1」カウント＋「置けば防げる」表示＋確定/防御成功の専用パルス（配置ミスの赤と判別） | 小〜中(3-4) | テキストのみ |
| 4 | 封印結果で「SAN 18 → 0」と変化後まで表示。SAN 0時はビネット最大収縮＋文字グリッチ→ゲームオーバーへ。残存SANで結果文字色変化 | 小〜中(3-4) | テキストのみ |
| 5 | タイトル→VN→資料→パズルに恐怖の橋: タイトル低頻度グリッチ・開始ボタンで赤明滅・パズル直前0.3秒暗転・遷移時BGMダッキング | 中(5-7) | なし |
| 6 | エンディングのプレースホルダ差し替え（3-5行の結末＋最終行の文字化け/赤転） | 小〜中(2-4) | 本文テキスト |
| 7 | 配置失敗演出の強度整理（発火条件は不変・低緊張時は表示時間/振幅を抑制、手の連鎖は高緊張時のみ） | 小(3) | なし |
| 8 | ギャラリーに怪異スチル切り替え表示（既存素材再活用・未解放カードもノイズ演出） | 小〜中(2-3) | なし |

### Codexトップ3: 提案1（タイムアップの理不尽解消・最小コスト）→ 提案2（既存演出資産への入力追加だけで危機言語を統一）→ 提案3（呪いを判断イベント化）

### Claude分析との突き合わせ
- **一致**: タイマー秒数＋終盤警告（Claude#1）・呪い警告の意味付け（Claude#3）。Codex版はカウントダウン・防御成功パルスまで踏み込んでおり上位互換
- **Codex独自**: 緊張度の合成値化（#2）・封印結果のSAN変化表示（#4）・遷移の恐怖の橋（#5）・ギャラリー再活用（#8）
- **Claude独自**: ゲームオーバー画面自体の演出強化（背景・BGM停止・フェードイン）→ Codex#4（死亡の瞬間の演出）と補完関係。gallery.gdのScreenTouch残存修正（サイクル1修正Bの取り残し）
- **対立**: Codex#7（配置失敗演出の強度整理）は「発火条件不変」と配慮されているが、スケア表示時間・振幅の変更はジャンプスケア演出の変更に該当し人間承認必須（前回フィードバック）。AI判断では実施しない

## UI・UX改善ループ サイクル1（2026-07-16）独立レビュー要約

**注**: Codex CLIが終了コード1で失敗（出力なし）したため、独立レビューは別コンテキストのClaudeサブエージェント（レビュアーにClaude側分析は非開示）が代行した。

### 提案一覧（レビュアー優先度順）
| # | 提案 | 対象 | 規模 | 素材 |
|---|------|------|------|------|
| 1 | 事件資料: 全面タップ即退出がスクロールと衝突し長文が読めない。「捜査を開始する」ボタン常設＋上部436px空白にヘッダ装飾 | case_file.gd:32-43 / .tscn:23-31 | 小(2) | なし |
| 2 | タップ送り画面全部でタッチ＋エミュレートマウスの二重発火疑い（1タップで全文表示＋次行へ）。MouseButtonのみに統一 | day_intro:153-164 / nightmare_event:183-196 / kaiki_still:60-76 / case_file:32-43 | 小(4) | なし |
| 3 | タイマーが細バーのみで数値なし→タイムアップ強制封印が「予告なしの事故」。数値表示＋残り25%脈動＋残り10秒警告 | hud.tscn:40-47 / hud.gd:65-72 / main_game.gd:180-186 | 小〜中(2-3) | なし |
| 4 | リセットが確認なしで全配置破棄（封印ボタンの隣・誤タップ代償大）。確認オーバーレイ。封印は未収納あり時のみ確認 | main_game.gd:363-393, 420-434 | 小(2) | なし |
| 5 | 配置ミス毎のフルスクリーンスケアの発火条件絞り込み → **人間承認必須のため保留推奨** | main_game.gd:312-322 | 小(1) | なし |
| 6 | ゲームオーバー画面が無演出（背景なし・BGM停止なし・即ボタン表示）。BGM停止＋赤ビネット＋フェードイン | game_over.tscn/.gd | 小(2) | なし |
| 7 | 呪いセル紫警告が無説明で初出（ガイドはDay1のみ）。初回のみヒント表示＋警告フラッシュ | grid_box.gd:383-390 / main_game.gd:171-178 | 小(2) | なし |
| 8 | 回転不可ピース再タップが無反応／危険度バッジ数字が無説明（"SAN-8"表記へ） | main_game.gd:261-266 / item_visual.gd:129-142 | 小(2) | なし |
| 9 | 広告リトライ後にVN＋事件資料を毎回全文再読。同一Day再訪時のみ「SKIP≫」ボタン | day_intro.gd / game_over.gd | 小〜中(2-3) | なし |
| 10 | SEプレイヤー1本で音が打ち消し合う（封印音がenter音で切れる）。4本プール化 | audio_manager.gd:43-47 | 小(1) | なし |

### レビュアーのトップ3
1. 提案1（事件資料の機能不全に近い問題・毎Day必ず通る）
2. 提案2（Android実機でVN送りが壊れている可能性・要実機検証）
3. 提案3（タイムアップの理不尽→「予告された恐怖」への転換。視認性・面白さ・演出を同時改善）

### Claude分析との突き合わせ
- **一致**: 事件資料の閉じる導線（Claude候補2）・タイマー数値表示（候補5）・ゲームオーバー演出強化（候補3）・VNテンポ改善（候補1はスキップ、レビューは同一Day再訪スキップでより的確）
- **レビュアー独自の発見**: タッチ二重発火疑い（#2）・リセット/封印の誤タップ（#4）・呪い警告の無説明（#7）・回転不可の無反応（#8）・SE切断（#10）。いずれもコードの行番号裏付けあり
- **Claude独自**: タイトル画面演出・設定画面のテキスト速度項目 → レビューでは挙がらず（優先度は下と判断）
- **対立**: 実質なし。提案5（スケア頻度）は前回の人間フィードバック（ジャンプスケア＝ホラーの核・変更は事前承認必須）に抵触するため、レビュアー自身も承認先行を明記


## サイクル1（2026-07-15）Codex独立レビュー要約

### 総評（Codex）
- Day 1〜3は「ピース総面積＝盤面面積」で全収納の達成感を作れる良い設計。配置プレビュー（緑/赤）・成功時SE/振動/パルスも良好。
- 最大の問題は「何をすべきか・どう回転するか・数字の意味」が一切説明されないこと。特に **Day 3は回転必須** なのに回転操作を知る手段がなく、「解けないパズル」に見える。

### Codexの問題点リスト（影響度順）
| # | 問題 | 影響度 | 検証状況 |
|---|------|--------|---------|
| 1 | 回転必須（Day 3）なのに回転操作の説明が皆無。`_rotate_active()` は未使用（main_game.gd:311） | 高 | Claude側でも確認（デッドコード確認済み） |
| 2 | Day 3: 残り30秒から呪いセルが15秒間隔で出現（main_game.gd:157-162）。16マス/16セル設計のため封鎖1つで全収納が数学的に不可能＝表示残り15秒で実質時間切れ。警告なし。リセットでも解除されない（grid_box.setup はstage初期値を再読込するが reset は clear_all_items のみ） | 高 | **Claudeがコードで裏取り済み・正確** |
| 3 | 目標（箱を埋める→封印）・SANダメージ・ピース上の数字の意味が事前に伝わらない。封印ボタンは開始直後から確認なしで押せる | 高 | 確認済み |
| 4 | 事件資料（case_files.json）の搬入物と実際のピースが不一致（Day1〜3全て）。物語とパズルが分離 | 高 | 未検証（次サイクルで確認） |
| 5 | 同一ItemDataリソースの複数ピース（Day2コイン×2、Day3紙×2）が個体として区別されず、別の個体が選択・回転・消失して見える | 中 | 未検証（item_tray.gdの一致判定はリソース比較であることは確認） |
| 6 | 配置失敗のたびに約1.2秒のフルスクリーンジャンプスケア（main_game.gd:287-291）。失敗理由の提示なし | 中 | **Claudeがコードで裏取り済み・正確** |
| 7 | VNテキスト送りが重い（スキップ・長押し早送りなし、導入約12秒＋終了約11秒/Day） | 中 | 確認済み |
| 8 | 事件資料画面はタッチ押下即遷移でスクロールと競合。「閉じる」ボタンなし | 中 | 未検証 |

### Codexの改善提案（優先度順）
1. Day 1に文脈型オンボーディング（操作を実際の順で教える＋封印ボタン付近に「未収納3・予想SAN −20」表示）【中】← 最優先推奨
2. Day 3の呪い発生の公平化（Day 4以降へ移す／事前カウントダウン／封鎖でなく警告。リセット時は動的封鎖を解除）【小〜中】
3. 事件資料とステージアイテムの同期（資料側の記述をピースに合わせるのが安全）【小】
4. トレイ項目に個体ID【中】
5. 配置失敗を短い学習フィードバックに（スケアはタイムアップ等に限定）【小〜中】
6. 封印確定前の状態明示（未収納時のみ確認）【小】
7. VNの高速閲覧手段【中】
8. 事件資料を明示ボタンで閉じる【小】

## サイクル3（2026-07-15）Codex技術レビュー要約（緊迫感演出の追加）

### 総評（Codex）
6件の要望はいずれもコアルール不変で実装可能。「残り時間から単一の緊迫度を算出し、画面・BGM・怪異頻度を一元制御する演出ディレクター」構成を推奨。10〜12ファイル規模。

### 重要な技術的発見（採用する）
1. **描画レイヤー**: JumpScareは `CanvasLayer layer=10`（jump_scare.tscn:11）で常に最前面。KaikiStill(z15)/NightmareEvent(z20)/PauseOverlay(z25)/HintLabel(z30)は既定レイヤー。
   → 新演出は**新規CanvasLayerを作らず**既定レイヤーの z_index=12〜14 に置く（通常画面とDragGhost(z10)は覆い、封印演出・ポーズには被らない）。※Claude当初案「新規CanvasLayer」は誤りのため破棄
2. **お化けの手のタイミング**: ミス時はジャンプスケア（0.7s保持+0.5sフェード）が手を覆うため、スケア終了後（約1.2秒後）に手を出す
3. **ポーズ安全性**: 独自ポーズ（_pausedフラグ）ではTween/Timer/シェーダーTIMEが止まらない。演出時計はMainGameの_processから渡すdeltaのみで進め、シェーダーへは effect_time をuniformで渡す。SEALED突入時に全演出とBGM pitchを明示リセット
4. **BGM**: pitch_scaleはテンポと音程の両方を変える（1.06〜1.08程度に抑制・平滑化）。volume_dbとは独立。AutoLoad残留対策としてシーン遷移・封印・無制限ステージで1.0へ復帰必須
5. **ピース演出**: Controlのrotation/scale/size/shapeは触らず `_draw()` 内の `draw_set_transform` と色だけ乱す。対象はItemDataでなくItemVisualインスタンスで識別（同一リソース複数個体問題の回避）。まずトレイ内のみ・同時1個
6. **不足素材**: お化けの手はリポジトリに存在せず。透明背景SVG（テキスト作成可）が最適。バイナリ必須なのは「写実的な手PNG」「緊迫BGM/ステム」「手専用SE」のみ→今回は見送り可
7. **性能**: 全画面演出は ColorRect 1枚＋シェーダー1本に統合。SCREEN_TEXTURE/ブラー不使用、UV距離とsin程度に留める（1080×2400のAndroid/Web対策）

### 対立点
- 実質なし。Claude当初案のうちCanvasLayer構成のみCodex指摘で修正。他（緊張度モデル・SVG・pitch_scale・_draw限定のピース異変）は一致。

---

### 対立点・訂正（サイクル1）
- **Claude側事前調査の誤りを訂正**: 事前調査エージェントは「Day 2=60秒制限・12セル>9マスで配置不可」と報告したが、一次確認の結果 **Codexが正しい**（game_manager.gd:169: day<=2 は時間無制限。day_02はコイン1セル×2+フック3+人形4=9セル=3×3で全埋め可能）。難易度カーブ自体は良設計。
- ジャンプスケアの評価: Claudeは「初見体験の最重要問題」、Codexは「影響度中」。ただし対処方針（通常ミスから除去しタイムアップ等に集約）は一致。
- 方向性の対立は実質なし。Codex独自の発見は #2（呪いセルの実質時間切れ）・#4（資料とピースの不一致）・#5（個体識別）で、いずれも妥当。
