# AI_REVIEW.md — Codexレビュー要約と突き合わせ

## サイクル1（2026-07-15）Codex独立レビュー要約

### 総評（Codex）
- Day 1〜3は「ピース総面積＝盤面面積」で全収納の達成感を作れる良い設計。配置プレビュー（緑/赤）・成功時SE/振動/パルスも良好。
- 最大の問題は「何をすべきか・どう回転するか・数字の意味」が一切説明されないこと。特に **Day 3は回転必須** なのに回転操作を知る手段がなく、「解けないパズル」に見える。

### Codexの問題点リスト（影響度順）
| # | 問題 | 影響度 | 検証状況 |
|---|------|--------|---------|
| 1 | 回転必須（Day 3）なのに回転操作の説明が皆無。`_rotate_active()` は未使用（main_game.gd:311） | 高 | Claude側でも確認（デッドコード確認済み） |
| 2 | Day 3: 残り30秒から呪いセルが15秒間隔で出現（main_game.gd:157-162）。16マス/16セル設計のため封鎖1つで全収納が数学的に不可能＝表示残り15秒で実質時間切れ。警告なし。リセットでも解除されない（grid_box.setup はstage初期値を再読込するが reset は clear_all_items のみ） | 高 | **Claudeがコードで裏取り済み・正確** |
| 3 | 目標（箱を埋める→封印）・SANダメージ・ピース上の数字の意味が事前に伝わらない。封印ボタンは開始直後から確認なしで押せる | 高 | 確認済み |
| 4 | 事件資料（case_files.json）の搬入物と実際のピースが不一致（Day1〜3全て）。物語とパズルが分離 | 高 | 未検証（次サイクルで確認） |
| 5 | 同一ItemDataリソースの複数ピース（Day2コイン×2、Day3紙×2）が個体として区別されず、別の個体が選択・回転・消失して見える | 中 | 未検証（item_tray.gdの一致判定はリソース比較であることは確認） |
| 6 | 配置失敗のたびに約1.2秒のフルスクリーンジャンプスケア（main_game.gd:287-291）。失敗理由の提示なし | 中 | **Claudeがコードで裏取り済み・正確** |
| 7 | VNテキスト送りが重い（スキップ・長押し早送りなし、導入約12秒＋終了約11秒/Day） | 中 | 確認済み |
| 8 | 事件資料画面はタッチ押下即遷移でスクロールと競合。「閉じる」ボタンなし | 中 | 未検証 |

### Codexの改善提案（優先度順）
1. Day 1に文脈型オンボーディング（操作を実際の順で教える＋封印ボタン付近に「未収納3・予想SAN −20」表示）【中】← 最優先推奨
2. Day 3の呪い発生の公平化（Day 4以降へ移す／事前カウントダウン／封鎖でなく警告。リセット時は動的封鎖を解除）【小〜中】
3. 事件資料とステージアイテムの同期（資料側の記述をピースに合わせるのが安全）【小】
4. トレイ項目に個体ID【中】
5. 配置失敗を短い学習フィードバックに（スケアはタイムアップ等に限定）【小〜中】
6. 封印確定前の状態明示（未収納時のみ確認）【小】
7. VNの高速閲覧手段【中】
8. 事件資料を明示ボタンで閉じる【小】

## サイクル3（2026-07-15）Codex技術レビュー要約（緊迫感演出の追加）

### 総評（Codex）
6件の要望はいずれもコアルール不変で実装可能。「残り時間から単一の緊迫度を算出し、画面・BGM・怪異頻度を一元制御する演出ディレクター」構成を推奨。10〜12ファイル規模。

### 重要な技術的発見（採用する）
1. **描画レイヤー**: JumpScareは `CanvasLayer layer=10`（jump_scare.tscn:11）で常に最前面。KaikiStill(z15)/NightmareEvent(z20)/PauseOverlay(z25)/HintLabel(z30)は既定レイヤー。
   → 新演出は**新規CanvasLayerを作らず**既定レイヤーの z_index=12〜14 に置く（通常画面とDragGhost(z10)は覆い、封印演出・ポーズには被らない）。※Claude当初案「新規CanvasLayer」は誤りのため破棄
2. **お化けの手のタイミング**: ミス時はジャンプスケア（0.7s保持+0.5sフェード）が手を覆うため、スケア終了後（約1.2秒後）に手を出す
3. **ポーズ安全性**: 独自ポーズ（_pausedフラグ）ではTween/Timer/シェーダーTIMEが止まらない。演出時計はMainGameの_processから渡すdeltaのみで進め、シェーダーへは effect_time をuniformで渡す。SEALED突入時に全演出とBGM pitchを明示リセット
4. **BGM**: pitch_scaleはテンポと音程の両方を変える（1.06〜1.08程度に抑制・平滑化）。volume_dbとは独立。AutoLoad残留対策としてシーン遷移・封印・無制限ステージで1.0へ復帰必須
5. **ピース演出**: Controlのrotation/scale/size/shapeは触らず `_draw()` 内の `draw_set_transform` と色だけ乱す。対象はItemDataでなくItemVisualインスタンスで識別（同一リソース複数個体問題の回避）。まずトレイ内のみ・同時1個
6. **不足素材**: お化けの手はリポジトリに存在せず。透明背景SVG（テキスト作成可）が最適。バイナリ必須なのは「写実的な手PNG」「緊迫BGM/ステム」「手専用SE」のみ→今回は見送り可
7. **性能**: 全画面演出は ColorRect 1枚＋シェーダー1本に統合。SCREEN_TEXTURE/ブラー不使用、UV距離とsin程度に留める（1080×2400のAndroid/Web対策）

### 対立点
- 実質なし。Claude当初案のうちCanvasLayer構成のみCodex指摘で修正。他（緊張度モデル・SVG・pitch_scale・_draw限定のピース異変）は一致。

---

### 対立点・訂正（サイクル1）
- **Claude側事前調査の誤りを訂正**: 事前調査エージェントは「Day 2=60秒制限・12セル>9マスで配置不可」と報告したが、一次確認の結果 **Codexが正しい**（game_manager.gd:169: day<=2 は時間無制限。day_02はコイン1セル×2+フック3+人形4=9セル=3×3で全埋め可能）。難易度カーブ自体は良設計。
- ジャンプスケアの評価: Claudeは「初見体験の最重要問題」、Codexは「影響度中」。ただし対処方針（通常ミスから除去しタイムアップ等に集約）は一致。
- 方向性の対立は実質なし。Codex独自の発見は #2（呪いセルの実質時間切れ）・#4（資料とピースの不一致）・#5（個体識別）で、いずれも妥当。
